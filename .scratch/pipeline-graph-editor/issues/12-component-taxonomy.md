Type: grilling
Status: open

## Question

Pin the component taxonomy: the 4 built-in components (Scanner, Quant Filter/Gate, AI Search, Report), their role order (R1–R4), and what config each owns. Decide:
- How the existing EP/VCP/BO/Custom screening families map onto the Scanner component — a `family` param? Separate scanner templates? Multiple gate groups in one panel?
- Whether Quant and Search are optional on a path (skip edges like Scanner → Report) and whether lanes may end at any component.
- Universe as the auto-seeded start node — confirm it stays off the palette.

Blocks: 14, 15, 16, 17.
