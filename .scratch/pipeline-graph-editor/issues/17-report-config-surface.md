Type: research
Status: open

## Question

Enumerate the Report component's configurable fields: report format, min_rating, down-only cap rules (reuse `apply_vcp_caps`), hard-cap ceilings (no-catalyst / PR / CONTRACT-FDA / RVOL10), and skip-enrich behavior (Report runs on scan/filtered rows with no context → `cap_applied=false`, `cap_reason="no_enrichment"`). Provide defaults + grouping.

Blocked by: 12.
