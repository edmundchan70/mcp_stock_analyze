Type: research
Status: open

## Question

Enumerate the AI Search component's configurable settings: Tavily dual-query (taxonomy + leadership query toggles), max_results, search_depth, time_range, topic, and the LLM compression prompt template. Provide defaults + grouping, sourced from `agents/enrichment.py` / `agents/catalyst.py` / `agents/rating.py`.

Blocked by: 12.
