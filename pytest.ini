[pytest]
markers =
    screener: tests covering the frozen/removed TradingView screener+OHLCV path
    integration: tests that call live external APIs (require API keys, skipped otherwise)
addopts = -m "not screener"
